myCourses <- c("MATH101", "STAT515", "ENG101", "MGMT100", "ECON681", "LEAD789", "ACOU610", "DATA630")
myCourses

length(myCourses)

myCourses[c(1,2)]

myCourses[c(3,4)]

sort(myCourses, decreasing = FALSE)

sort(myCourses, decreasing = TRUE)